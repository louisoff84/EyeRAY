# EyeRAY 1.1.0
# Universal anti-XRay for Bukkit/Spigot/Paper 1.8.8 through Paper 26.2.
# The real world is never modified: EyeRAY only sends fake block changes to clients.

enabled: true

# How many chunks around each player are protected.
# 1 = 3x3 chunks, 2 = 5x5 chunks.
chunk-radius: 1

# Real blocks are revealed when the player gets this close.
reveal-distance: 6.0

# Main player update interval. Lower values reveal blocks faster but cost more CPU.
refresh-interval-ticks: 5

# Re-send obfuscation periodically so unrelated block updates cannot expose ores forever.
reapply-interval-ticks: 100

# Number of previously uncached chunks that may be scanned in one server tick.
scan-budget-per-tick: 1

# Vertical range to scan. EyeRAY automatically clamps this to the server world's real height.
# 1.8.8-1.16 worlds start at Y=0; modern worlds can use negative Y.
scan:
  min-y: -64
  max-y: 128

# If false, ores touching air/water/lava/non-solid blocks remain visible.
hide-exposed-ores: false

# Materials hidden from XRay clients.
# Names unavailable on the running Minecraft version are ignored automatically.
protected-blocks:
  - COAL_ORE
  - DEEPSLATE_COAL_ORE
  - COPPER_ORE
  - DEEPSLATE_COPPER_ORE
  - IRON_ORE
  - DEEPSLATE_IRON_ORE
  - GOLD_ORE
  - DEEPSLATE_GOLD_ORE
  - REDSTONE_ORE
  - DEEPSLATE_REDSTONE_ORE
  - LAPIS_ORE
  - DEEPSLATE_LAPIS_ORE
  - DIAMOND_ORE
  - DEEPSLATE_DIAMOND_ORE
  - EMERALD_ORE
  - DEEPSLATE_EMERALD_ORE
  - NETHER_GOLD_ORE
  - NETHER_QUARTZ_ORE
  - QUARTZ_ORE
  - ANCIENT_DEBRIS

# Optional Orebfuscator-like noise: fake ores are placed only client-side.
decoys:
  enabled: true
  max-per-chunk: 24
  chance-per-10000: 18
  seed-salt: 918273645
  base-blocks:
    - STONE
    - DEEPSLATE
    - NETHERRACK

worlds:
  # BLACKLIST = all worlds except listed ones.
  # WHITELIST = only listed worlds.
  mode: BLACKLIST
  list: []

messages:
  prefix: '&8[&bEyeRAY&8] &7'
  no-permission: '&cTu n''as pas la permission.'
  reloaded: '&aConfiguration rechargee et cache vide.'
  enabled: '&aProtection activee.'
  disabled: '&cProtection desactivee. Les faux blocs ont ete restaures.'
